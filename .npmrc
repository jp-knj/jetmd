# Enable pnpm side-effects cache for faster native builds
side-effects-cache=true
side-effects-cache-readonly=false
save-exact=true

# Publishing placeholders (replace in CI or local shell before running release:build)
registry=https://registry.npmjs.org/
//registry.npmjs.org/:_authToken=${NPM_TOKEN:-simulate-token}
; To publish scoped packages, uncomment and set registry: @jetmd:registry=https://registry.npmjs.org/
